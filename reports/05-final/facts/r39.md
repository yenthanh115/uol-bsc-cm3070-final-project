# R39 — Report and code agree

**Requirement (CRITICAL):** Feature names, dataset counts, windows, model settings and reported metrics match the frozen implementation/results.

**Verdict: PASS**

Each category the requirement names is cross-checked between the report and the actual `/src` code plus persisted result artefacts.

## 1. Feature names — agree

`src/surge_pipeline/features.py` `FEATURE_COLUMNS` lists exactly the 11 features in report `@tbl:feature-definitions`, same names:

`sentiment_score`, `hour_of_day`, `day_of_week`, `time_since_previous`, `ticker_post_rate_24h`, `ticker_post_acceleration`, `word_count`, `title_length`, `num_tickers_mentioned`, `word_count_x_hour`, `accel_x_time_since_prev` (9 base + 2 interaction).

The code comment ("9 base + 2 interactions") and docstring ("Engagement metrics (score, num_comments) are explicitly excluded") match the report's exclusion claim in `@sec:feature-engineering` and the test `test_features_exclude_score_and_num_comments`.

## 2. Dataset counts — agree

`output/evaluation/2026-07-19_08-28_evaluation_metrics.json` (the WSB final run, seed 42) reports `support_positive = 668` and `support_negative = 68255` → 68,923 test records, matching `@tbl:dataset-characteristics` and `@tbl:perf-default` (WSB: 68,923 records, 668 surges). The labelling log for the same run family records `train_size 388149 / test_size 68923`, matching the report's WSB train/test split. pennystocks `train_size 21549 / test_size 3278` in the log matches the report's 21,549 / 3,278.

## 3. Windows — agree

- `src/surge_pipeline/config.py`: `WINDOW_SECONDS = 24 * 60 * 60` (24h) — the report's 24-hour forward/backward window (`@sec:surge-definition`, `@sec:data-representation`).
- `windowing.py` and `sentiment.py` both import and use `WINDOW_SECONDS` for backward `[t-24h, t)` and forward `(t, t+24h]` counts via `np.searchsorted`, matching the report's window definitions and boundary semantics.
- `features.py`: `_12H_SECONDS`/`_24H_SECONDS` implement the 12h/24h split for `ticker_post_acceleration`, matching `@tbl:feature-detail` row 3.
- `temporal_split_ratio = 0.8` (80/20) matches `@sec:temporal-validation` Level 1; `N_FOLDS = 4` matches the expanding-window k=4.

## 4. Model settings — agree

- `_make_lr`: `solver` Elastic Net, `class_weight="balanced"`, `max_iter=5000` — matches `@lst:model-factory` and `@tbl:model-families` (LR Elastic Net baseline).
- RF grid `_get_rf_param_grid`: `n_estimators ∈ {50,100,200}` × `max_depth ∈ {3,5,10,None}` × `min_samples_leaf ∈ {1,2,5}` = 36 configs — matches `@tbl:hyperparams` (RF grid size 36).
- XGB grid `_get_xgb_param_grid`: `n_estimators ∈ {50,100,200}` × `max_depth ∈ {3,5,7}` × `learning_rate ∈ {0.01,0.1,0.3}` × `scale_pos_weight ∈ {1, ratio/2, ratio}`, capped at 50 — matches `@tbl:hyperparams` (XGB "≤50") and `@lst:imbalance-ratio`.
- `threshold_tau = 1.5` default matches the report's primary operating point.

## 5. Reported metrics — agree

WSB default-threshold metrics in `@tbl:perf-default` vs persisted `2026-07-19_08-28_evaluation_metrics.json`:

| Model | Report (AUC / P / R / F1) | JSON (AUC / P / R / F1) | Match |
|---|---|---|---|
| LR | 0.707 / 0.013 / 0.801 / 0.026 | 0.7065 / 0.0132 / 0.8009 / 0.0260 | Yes |
| RF | 0.880 / 0.095 / 0.311 / 0.145 | 0.8801 / 0.0947 / 0.3114 / 0.1453 | Yes |
| XGB | 0.892 / 0.043 / 0.819 / 0.081 | 0.8917 / 0.0426 / 0.8189 / 0.0810 | Yes |

Further corroboration from the experiment log (see also r38.md): pennystocks RF AUC 0.753 (logged 0.7527/0.7539), Phase 1 volume-only 0.710, the full weight sweep (0.708/0.861/0.872), and cross-transfer 0.684/0.871 all match their report tables.

## Summary

| Category | Report source | Code / artefact source | Agree? |
|---|---|---|---|
| Feature names | `@tbl:feature-definitions` | `features.py` `FEATURE_COLUMNS` | Yes |
| Dataset counts | `@tbl:dataset-characteristics` | eval JSON support counts + experiment log | Yes |
| Windows | `@sec:surge-definition`, `@sec:temporal-validation` | `config.WINDOW_SECONDS`, `temporal_split_ratio`, `N_FOLDS` | Yes |
| Model settings | `@tbl:hyperparams`, `@lst:model-factory` | `training.py` factories + grids | Yes |
| Reported metrics | `@tbl:perf-default` (and others) | persisted evaluation JSON | Yes |

**R39 = PASS**
