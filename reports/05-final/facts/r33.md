# R33 — Pipeline architecture is documented visually

**Requirement (MUST):** Clear diagram shows data → preprocessing → aggregation/features → labels → model → prediction/evaluation.

**Verdict: PASS**

## Report evidence

- `@sec` "Overall Pipeline Architecture": the system is described as a "six-stage linear pipeline" where "Each stage consumes the previous stage's output and writes intermediate artefacts to disk."
- `@tbl:pipeline-stages` enumerates the six stages and responsibilities, covering the full requirement chain:
  1. Data Loading
  2. Windowing (preprocessing / aggregation)
  3. Sentiment
  4. Target Labelling (labels)
  5. Feature Engineering (features)
  6. Model Training and Evaluation (model → prediction/evaluation)
- `@fig:pipeline` — `figures/fig2-data-pipeline-v0.1.png` — the pipeline architecture diagram. Caption notes shading for critical design points: target labelling (leakage prevention), model training (temporal validation), and evaluation (statistical rigour). **Confirmed present on disk.**
- `@fig:expanding-cv` — `figures/fig3-expanding-window-cv.png` — supports the model-training stage by visualising the expanding-window CV scheme. **Confirmed present on disk.**
- Implementation Status `@sec`: "All six pipeline stages ([@tbl:pipeline-stages]) are fully implemented and execute end-to-end on both datasets."

## Code evidence (the stages the diagram depicts exist in `/src`)

| Diagram stage | Source |
|---|---|
| Data Loading | `src/surge_pipeline/loader.py` |
| Windowing / preprocessing | `src/surge_pipeline/windowing.py` |
| Sentiment | `src/surge_pipeline/sentiment.py` |
| Target Labelling | `src/surge_pipeline/labelling.py`, `run_labeling.py` |
| Feature Engineering | `src/surge_pipeline/features.py` |
| Model Training & Evaluation | `src/surge_pipeline/training.py` (expanding-window CV), `src/surge_pipeline/evaluation.py`, `run_training.py`, `run_cross_validation.py` |
| Orchestration | `src/surge_pipeline/pipeline.py` |

The visual documentation (`fig2-data-pipeline-v0.1.png`) accurately reflects the implemented pipeline: each depicted stage maps to a concrete module in `/src`, and orchestration in `pipeline.py` wires the stages in the same data → preprocessing → features → labels → model → evaluation order shown.

## Summary

| Check | Result |
|---|---|
| Diagram present on disk | Yes (`figures/fig2-data-pipeline-v0.1.png`) |
| Covers data → preprocessing → features → labels → model → evaluation | Yes (6-stage table + diagram) |
| Diagram matches implemented code | Yes (each stage → module in `/src`) |
| Supporting CV figure present | Yes (`fig3-expanding-window-cv.png`) |

**R33 = PASS**
