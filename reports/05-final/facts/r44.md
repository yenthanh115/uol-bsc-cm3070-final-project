# R44 — Demo proves the product works

**Requirement (MUST):** Demo shows representative input/data, execution or pipeline behaviour, outputs/predictions and evaluation rather than only slides explaining the idea.

**Verdict: NOT PASS (demo artefact for the final system is absent / could not be verified)**

Status in `requirements.md` is left blank (not marked PASS).

## What R44 requires

A demonstration (per `reports/05-final/instruction.md`, a 3–5 minute spoken video) that actually shows the *working final project*: representative input data, the pipeline executing, produced outputs/predictions, and evaluation — not just slides.

## What exists in the repository

- `reports/03-preliminary-report/demo/demo-plan.md` — a 4-minute **demo script/plan**, but it belongs to the **preliminary-report** stage and describes a **superseded prototype**, not the final system.
- No video file of any kind is present: a repo-wide search (excluding `.venv`) for `.mp4` / `.mov` / media returns nothing.
- The final report (`final-report-v0.0.2.md`) contains **no link** to a hosted demo video (no YouTube/Vimeo/URL).

## Why the existing plan does not satisfy R44

The preliminary demo plan describes a materially different (older) system than the frozen final report:

| Aspect | Preliminary demo-plan.md | Final report (frozen) |
|---|---|---|
| Datasets | `r/pennystocks` only (~55k posts) | Two: `r/pennystocks` + `WSB` |
| Usable records | "~8,500", "~89% excluded" | 24,827 (pennystocks) / 457,072 (WSB) |
| Models | Logistic Regression only | LR + Random Forest + XGBoost |
| Features | 9 features | 11 features (9 base + 2 interaction) |
| Headline metric | ROC-AUC 0.733 (LR) | XGB 0.892 (WSB), RF 0.753 (pennystocks) |
| Evaluation depth | confusion matrix + ROC | + bootstrap CIs, McNemar's, cross-dataset transfer, sensitivity sweeps |

So even as a script, it does not demonstrate the final product's features, results, or evaluation.

## What is needed to make R44 PASS

- A final 3–5 minute demonstration video (with spoken audio, per `instruction.md`) that shows: the raw input data, the pipeline running end-to-end (`surge-label` → `surge-train` / `run_cross_validation.py`), the produced predictions/outputs, and the evaluation (metrics/figures) for the **final** two-dataset, three-model system.
- The artefact (or a link to it) present in the submission package so it can be verified.

This is a submission artefact that cannot be evidenced from the report text or `/src` code alone; it must be produced and included.

## Summary

| Check | Result |
|---|---|
| Final demo video present in repo | No |
| Link to hosted demo video in report | No |
| Existing demo artefact matches final system | No (preliminary prototype only) |
| Shows data → execution → outputs → evaluation of final product | Not verifiable |

**R44 = NOT PASS** (blocked on a final demo video demonstrating the working final system).
