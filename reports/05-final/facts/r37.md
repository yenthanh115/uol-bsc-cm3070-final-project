# R37 — Repository is submission-ready

**Requirement (MUST):** Dependencies, configuration, execution instructions, paths, seeds and required data/resources are documented.

**Verdict: PASS**

## Repository evidence (verified on disk)

The requirement is satisfied primarily by the actual repository state, corroborated by the report's Implementation section.

### Dependencies — documented

- `requirements.txt` (repo root) pins the full dependency set: `pandas>=2.0`, `numpy>=1.24`, `matplotlib>=3.7`, `seaborn>=0.12`, `scikit-learn>=1.3`, `scipy>=1.10`, `textblob>=0.17`, `vaderSentiment>=3.3`, `xgboost>=1.7`, `joblib>=1.2`, `tqdm>=4.60`, plus dev/test tooling (`pytest`, `mypy`, `ruff`, `pandas-stubs`) and EDA data-access (`kaggle`, `huggingface_hub`).
- `pyproject.toml` (repo root) declares the same runtime dependencies under `[project.dependencies]`, an optional `dev` extra, `requires-python = ">=3.10"`, and packaging metadata (`name = "surge-pipeline"`, `version = "0.1.0"`, MIT license).
- Report §"Code Organisation" states versions are recorded in `requirements.txt` and logged per run.

### Configuration — documented

- `pyproject.toml` centralises tool configuration: `[tool.pytest.ini_options]` (testpaths, pythonpath), `[tool.ruff]` / `[tool.ruff.lint]`, and `[tool.mypy]` with per-module overrides.
- `README.md` §"Configuration" documents `PipelineConfig` (in `src/surge_pipeline/config.py`) and shows a full JSON config example (split ratio, `min_window_count`, `threshold_tau`, weights, thresholds, `random_seed`).
- Report §"Code Organisation" describes central control via the `PipelineConfig` dataclass, overridable by config files.

### Execution instructions — documented

- `README.md` provides end-to-end instructions: Prerequisites (Python 3.10+), Setup (venv creation/activation for Windows cmd, PowerShell, macOS/Linux), `pip install -r requirements.txt`, and TextBlob corpora download.
- Usage sections cover every stage: labelling (`run_labeling.py`), training (`run_training.py`), EDA (`eda.eda_pipeline`), tests (`pytest`), figure generation (`generate_figures.py`), and cross-dataset validation (`run_cross_validation.py`), each with flag tables and defaults.
- `pyproject.toml` `[project.scripts]` exposes console entry points: `surge-label`, `surge-train`, `surge-cross-val`, `surge-figures`, `surge-examples` — matching the report's `@tbl:cli-entry-points`.

### Paths — documented

- `README.md` §"Project Structure" documents the full `input/` (read) vs `output/` (write) layout, and the `src/` module/CLI organisation, matching `@lst:source-org` in the report.
- CLI flag tables document input/output path arguments (`--file-path`, `--output-dir`, `--data-path`, `--models-dir`, `--figures-dir`, etc.) with defaults.

### Seeds — documented

- `README.md` documents `--random-seed` (default `42`) and the JSON config `random_seed: 42`.
- Report §"Code Organisation": "a fixed global seed (default 42) is systematically set across Python's native random module, NumPy, and all scikit-learn estimators."
- Report §"Implementation Status" reports determinism verified across seeds {42, 123, 456, 789, 2024}.

### Required data / resources — documented

- `README.md` names the required raw input (`input/raw/r_pennystocks_submissions_reddit.csv`), the TextBlob corpora download step, and the produced output artefacts (`labelled_dataset.csv`, `evaluation_metrics.json`, `final_summary.json`, figures, `experiment_log.jsonl`).
- Report §"EDA Tooling" documents dataset provenance (`leukipp/reddit-finance-data` [21]) and the Kaggle/HuggingFace access path.

### CLI scripts exist (verified via directory listing of `src/`)

`run_labeling.py`, `run_training.py`, `run_cross_validation.py`, `generate_figures.py`, `generate_prediction_examples.py`, plus `surge_pipeline/` package and `tests/` — all present, matching what the README and `pyproject.toml` reference.

## Summary

| Submission-readiness element | Documented? | Location |
|---|---|---|
| Dependencies | Yes | `requirements.txt`, `pyproject.toml` |
| Configuration | Yes | `PipelineConfig`, README §Configuration, `pyproject.toml` tool config |
| Execution instructions | Yes | README Setup/Usage; `[project.scripts]` |
| Paths | Yes | README Project Structure + CLI flag tables |
| Seeds | Yes | README (`--random-seed` 42); report §Code Organisation |
| Required data / resources | Yes | README (raw CSV, TextBlob corpora); report §EDA Tooling |
| Referenced CLI scripts present on disk | Yes | `src/run_*.py`, `generate_*.py` |

**R37 = PASS**
