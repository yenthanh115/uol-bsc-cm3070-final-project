<<<<<<< HEAD
# Research Journal — 2026-07-01
=======
# Research Journal — 2026-07-08
>>>>>>> 008a5991c10ef54cf0069cae6071c1aa5899c74a

## Session focus

Phase 1 completion: Foundation & Folder Reorganisation.

## Objective

Establish a clean project structure, implement the full multi-model training and evaluation interface, and get the test suite passing. Phase 1 is the critical foundation that all subsequent phases build upon — it sets path conventions and makes the codebase testable.

## Tasks completed

### [x] 1.1 — Reorganise input/output folders
- Moved from scattered layout to a clear `input/` (immutable reads) and `output/` (reproducible writes) separation.
- Created `input/raw/`, `input/reference/`, `output/processed/`, `output/models/`, `output/evaluation/`, `output/figures/eda/`, `output/figures/evaluation/`.
- Moved raw CSV to `input/raw/`, labelled dataset to `output/processed/`, EDA figures (01–09) to `output/figures/eda/`, evaluation figures (10–12) to `output/figures/evaluation/`.
- Removed old `data/` and `figures/` directories.
- Added `.gitkeep` in empty tracked folders.
- Updated `.gitignore` to ignore large CSVs and model files while keeping JSON configs tracked.

### [x] 1.2 — Update all code path references
- `PipelineConfig` defaults updated: `file_path` → `input/raw/...`, `output_dir` → `output/processed`.
- `evaluation.py` `FIGURES_DIR` → `output/figures/evaluation`.
- `eda_pipeline.py` figure output → `output/figures/eda`.
- `run_pipeline.py` and `run_training.py` default CLI paths updated.
- `README.md` project structure and usage examples updated.

## Status

COMPLETED
