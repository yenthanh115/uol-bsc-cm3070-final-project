# Research Journal — 2026-06-17

## Session focus

Decision log audit and gap analysis. Ensuring all critical design decisions are formally documented for academic traceability.

## Objective

Review the existing decision log and journals to identify undocumented design choices that an examiner would expect to see justified. Fill gaps with formal DEC entries following the established format.

## Audit findings

Reviewed all journals (2026-05-18 through 2026-07-07) and the decision log (DEC-001 through DEC-013). Found 7 critical decisions that were made implicitly during implementation but never formally recorded:

| Gap | Risk if undocumented |
|-----|---------------------|
| Why LR as baseline model | Examiner will ask "why not SVM or Naive Bayes?" |
| Why these three models (LR + RF + XGB) | No rationale for excluding neural networks or LightGBM |
| Why TextBlob initially | Appears arbitrary without documented trade-off analysis |
| Why 80/20 temporal split | Ratio choice and no-stratification need justification |
| Why expanding-window CV over sliding-window | Key methodological choice with no formal record |
| Feature set design rationale | 9 features chosen but "why these 9?" undocumented |
| Hyperparameter grid sizes | Grid limits appear arbitrary without compute budget reasoning |

## Decisions added

Added DEC-014 through DEC-020 to `admin/decision-log.md`:

- **DEC-014** (2026-06-12): Logistic Regression as baseline — interpretability, linear lower bound, standard in literature, fast training, calibrated probabilities
- **DEC-015** (2026-06-12): Model selection (LR + RF + XGB) — paradigm coverage, tabular data suitability, XGBoost over LightGBM, compute budget constraints
- **DEC-016** (2026-06-13): TextBlob as initial sentiment tool — simplicity, reproducibility, known-baseline strategy, zero-configuration, planned VADER migration
- **DEC-017** (2026-06-13): Temporal 80/20 split — no leakage, realistic evaluation, sufficient test size for bootstrap CIs, concept drift acknowledged
- **DEC-018** (2026-06-15): Expanding-window CV (k=4) — respects temporal ordering, data-efficient vs sliding, fold size adequate for AUC stability
- **DEC-019** (2026-06-15): Feature set (9 leakage-free features) — three signal categories, no post-creation aggregates, minimal count reduces overfitting
- **DEC-020** (2026-06-17): Grid sizing — exhaustive for LR/RF (small grids), randomised ≤50 for XGBoost, Bergstra & Bengio justification

## Chronological reordering

Fixed one ordering issue: DEC-013 (2026-06-11, z-score normalisation) was listed after DEC-012 (2026-06-12, leakage elimination). Reordered both the TOC and body sections so entries flow chronologically. Dates in body text aligned with TOC dates.

## Observations

- The decision log now covers all choices an examiner is likely to challenge: target definition, model selection, feature engineering, evaluation strategy, and hyperparameter tuning.
- Backdating the new entries to their actual decision dates (June 12–17) rather than today's date preserves honest chronology — these decisions *were* made on those dates, just not formally recorded.
- Each new entry follows the established format: context → decision → rationale → alternatives considered → status.

## Status

COMPLETED
