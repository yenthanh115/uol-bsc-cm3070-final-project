# Research Journal — 2026-07-03

## Session focus

Phase 1 completion: Foundation & Folder Reorganisation.

## Objective

Establish a clean project structure, implement the full multi-model training and evaluation interface, and get the test suite passing. Phase 1 is the critical foundation that all subsequent phases build upon — it sets path conventions and makes the codebase testable.

## Tasks completed

### [x] 1.3 — Refactor `training.py` to match test interface
- Exposed: `create_temporal_folds`, `get_expanding_window_splits`, `N_FOLDS`, `_verify_temporal_ordering`, `_get_lr_param_grid`, `_get_rf_param_grid`, `_get_xgb_param_grid`, `train_models`, `get_training_summary`.
- Introduced `TrainedModel` dataclass returned per model, with `.model`, `.scaler`, `.cv_scores`, `.best_params`.
- Existing LR logic wrapped into the new multi-model structure.

### [x] 1.4 — Implement Random Forest and XGBoost training
- Added `RandomForestClassifier` with grid: `n_estimators(3) × max_depth(4) × min_samples_leaf(3) = 36` configs.
- Added `XGBClassifier` with grid ≤50 configs.
- Reused temporal CV infrastructure (expanding-window splits, AUC-ROC selection, retrain on full training set).
- Added `xgboost` to `requirements.txt`.

